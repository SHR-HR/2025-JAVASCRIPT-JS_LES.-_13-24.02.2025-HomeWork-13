//=========================================================================================================
// Домашнее задание.
//=========================================================================================================
// Курс:    Разработка интерфейса на JavaScript. 
// Дисциплина:  Основы  JavaScript. 
//=========================================================================================================
// Домашнее задание №13 : Обработка ошибок. try, catch.
//=========================================================================================================
// Решите данную задачу, используя обработчик ошибок.
//=========================================================================================================
// Сверстайте свою форму для регистрации (html + css + js). 
// Минимум должно быть 5 полей. 
// Обязательно проверьте каждое поле на все типы ошибок. 
// Например, поля для телефона должно состоять только из цифр и т.д. 
// После проверки всех данных, запишите все данные в объект и выведите результат.
//=========================================================================================================

console.log("=== Домашнее задание №13: Обработка ошибок. try, catch ===");
// --------------------------------------------------------------------------------------------------------
function validateForm() {
    try {
        console.log("🚀 Начало валидации формы...");
// --------------------------------------------------------------------------------------------------------
        // Получаем значения из формы
        let name = document.getElementById("name").value.trim();
        let email = document.getElementById("email").value.trim();
        let phone = document.getElementById("phone").value.trim();
        let password = document.getElementById("password").value;
        let confirmPassword = document.getElementById("confirmPassword").value;
// --------------------------------------------------------------------------------------------------------
        // Проверяем имя
        if (!name) throw new Error("Имя не может быть пустым!");
        if (name.length < 2) throw new Error("Имя слишком короткое!");
        if (!/^[а-яА-Яa-zA-Z]+$/.test(name)) throw new Error("Имя должно содержать только буквы!");
        if (name.includes(" ")) throw new Error("Имя не должно содержать пробелы!");
// --------------------------------------------------------------------------------------------------------
        // Проверяем email
        if (!email) throw new Error("Email не может быть пустым!");
        if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) throw new Error("Неверный формат email!");
// --------------------------------------------------------------------------------------------------------
        // Проверяем телефон
    if (!phone) throw new Error("Телефон не может быть пустым!");
    if (!/^\+?[7-9]\d{9,11}$/.test(phone)) throw new Error("Телефон должен содержать 10-12 цифр и начинаться с +7 или 7");
// --------------------------------------------------------------------------------------------------------
        // Проверяем пароль
        if (!password) throw new Error("Пароль не может быть пустым!");
        if (password.length < 6) throw new Error("Пароль должен содержать минимум 6 символов!");
        if (!/[A-Z]/.test(password)) throw new Error("Пароль должен содержать хотя бы одну заглавную букву!");
        if (!/\d/.test(password)) throw new Error("Пароль должен содержать хотя бы одну цифру!");
// --------------------------------------------------------------------------------------------------------
        // Проверяем подтверждение пароля
        if (password !== confirmPassword) throw new Error("Пароли не совпадают!");
// --------------------------------------------------------------------------------------------------------
        // Если все ок – создаем объект пользователя
        let user = {
            name,
            email,
            phone,
            password
        };

        console.log("✅ Регистрация успешна!", user);
        document.getElementById("result").innerHTML = "✅ Регистрация успешна!";
// --------------------------------------------------------------------------------------------------------
        // Дополнительно выводим информацию о браузере и системе
        console.log("🌐 Браузер:", navigator.userAgent);
        console.log("🖥️ Платформа:", navigator.platform);
        console.log("📡 Онлайн статус:", navigator.onLine ? "Онлайн" : "Оффлайн");
        console.log("🌍 Текущий URL:", window.location.href);
        console.log("🕒 Время выполнения:", new Date().toLocaleString());
        
    } catch (error) {
        console.error("❌ Ошибка:", error.message);
        document.getElementById("result").innerHTML = `❌ Ошибка: ${error.message}`;
    }
}
// --------------------------------------------------------------------------------------------------------


